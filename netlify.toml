[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install && pnpm --filter @applyme/web build"
  publish = ".next"
  environment = { SECRETS_SCAN_OMIT_KEYS = "APP_BASE_URL" }

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "8"
