[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install && pnpm --filter @applyme/web build"
  publish = "apps/web/.next"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "8"
